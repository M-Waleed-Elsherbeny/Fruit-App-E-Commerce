class CardItemModel {
  final String image, name, rating, reviews, price;
  CardItemModel({
    required this.image,
    required this.name,
    required this.rating,
    required this.reviews,
    required this.price,
  });
}
